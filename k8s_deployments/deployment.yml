apiVersion: apps/v1
kind: Deployment
metadata:
  name: abstechnologies_ansible-deployment
spec:
  replicas: 2
  selector:
    matchLabels:
      app: abstechnologies_ansible
  template:
    metadata:
      labels:
        app: abstechnologies_ansible
    spec:
      containers:
        - name: abstechnologies_ansible
          image: mahshamim/abstechnologies_ansible:latest
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "prod"  # Specify the active profile if needed